<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Plugin.Maui.ShellTabBarBadgeSample.HomePage">
    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="icon.png"
                HeightRequest="185"
                Aspect="AspectFit" />

            <Label
                Text="Hello, TabBar Badge!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="The plugin that let you have badges in your MAUI Shell app"
                Style="{StaticResource SubHeadline}" />


            <Button
                Text="Reset Badges" 
                Clicked="OnResetBadgesClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>